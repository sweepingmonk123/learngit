[[QA]]
= 常见问题

== 实时任务

== 离线任务


=== 数据同步


==== 如何提高MySQL、Oracle数据库的数据同步速度？

* 我这有个任务场景，不知道任务依赖如何处理，我有一张用户表，一个月更新一次，每次更新的时候先清空表数据，然后同步，这样依赖关系如何处理

* 了解了，那么有个删除的语句，delete * from nbcei_robot.core_user;这样的正确吗
牧云



* @牧云 ，能不能把MR这个任务开发的说明详细些，就是数据怎么接入，jar包如何调用，结果的输出

* @牧云 ，我要计算两个表的两个字段的和或差，直接用SQL任务还是MR？

* 如果任务定的起调时间是20点，那我现在补数据操作也要到20点执行？
牧云

* 内部表，外部表的解释
内部表和外部表有什么区别，用起来的时候区别就在于外部表不会删除真的数据；
内部表，只能在当前项目空间内；
外部表可以随便指定路径，外部表，删除的时候不会删除外表的数据；


* 发布和运维是什么概念

* 提示信息
选择内部表就可以把
思枢
选择了内部表，你的数据只能通过insert 进去，不能通过外部的文件导入了

* 如何立即运行

* 只执行一次怎么办

* @牧云 ，我这有个任务场景，不知道任务依赖如何处理，我有一张用户表，一个月更新一次，每次更新的时候先清空表数据，然后同步，这样依赖关系如何处理

* 我一个同步的任务，这个表天天会有数据更新，在没有做按时间过滤的情况下，同步到HDFS上，会如何处理？
hive 有分区的，所以你把你每天的数据同步到不同的分区上就可以了


* 有时候会忘记发布了
也就是说我每次对任务的修改，保存后，应该发布


* 还有前两天和你说的一些一次性的任务，我现在是勾选了停止的，但是在第二天，都是以任务失败的状态，你看看怎么设计，把这些状态改掉，不然正式用的时候，人家都以为平台发生什么问题了呢

* 还有个问题，能正确的告知一下，分区的建立以及数据同步时候如何对应分区

* 我有一张客户表，有个字段是create_time的，在可视化建表的时候，用create_time字段作为分区字段，那么在同步数据的时候，和正常的一样？
是的,只要你建好这个分区表，会自动把相应的数据导入到对应的分区上的


调度参数那里差异比较大，要好好梳理

rdos不支持在代码中引用资源

rdos与阿里云的告警逻辑不一样

虚节点的实现逻辑不一样，我们是手动创建虚节点，阿里云是默认的虚节点

上游任务失败后，下游任务也会失败
